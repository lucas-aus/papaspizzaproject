Upon opening the program you will be given the choice to order something or to look at an order summary.
You will need to use the order function first, so that the order summary has data to summarise.

If using the order summary feature, a password will be required to access the feature.
The password is currently set to "employeepassword".